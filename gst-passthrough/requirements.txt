# Pinned to the versions the published figures were produced with.
# Python 3.11.15
#
# lunardate is required by 07, 08, 09 and 14, which compute Chinese New Year dates.
# Without it those scripts fail with ModuleNotFoundError on a clean machine.

pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
statsmodels==0.15.0
lunardate==0.3.0
